{
    "extends": "airbnb/base",
    "rules": {
        "indent": [2, <% if (indentation == 'tabs') { %>"tab"<% } else { %><%= indentationSpaces %><% } %>],
        "space-before-function-paren": [2, {
            "anonymous": "always",
            "named": "never"
        }]
    }
}
